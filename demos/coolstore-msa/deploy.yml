---
- name: Deploy Coolstore Microservices Demo
  hosts: localhost
  gather_facts: false
  run_once: true
  roles:
    - coolstore_projects
    - { role: nexus, desired_project: "{{project_cicd}}", when: maven_mirror_url == '' }
    - { role: gogs, desired_project: "{{project_cicd}}" }